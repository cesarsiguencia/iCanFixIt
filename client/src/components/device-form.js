const DeviceForm = ({clientId}) =>{
    return(
        <div>
            <p>The device form's id is the following:</p>
            <p>{clientId}</p>
            
        </div>
    )
}

export default DeviceForm